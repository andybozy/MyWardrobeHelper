# Copy this file to `.codex/config.toml` in a trusted checkout.

[mcp_servers.mywardrobehelper]
command = "cargo"
args = ["run", "--quiet", "--release", "--", "mcp", "connect", "--data-dir", ".data"]
cwd = "."
