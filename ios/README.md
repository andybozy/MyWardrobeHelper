# iOS Workspace Notes

The native iOS companion app lives under `ios/MyWardrobeHelperiOS/`.

Current state:

- Xcode project exists as a native SwiftUI placeholder
- LAN server profiles, API client work, item screens, and uploads are tracked in `SEC-011` through `SEC-015`

The Rust backend remains the source of truth. The iOS app will only talk to the backend JSON API.
